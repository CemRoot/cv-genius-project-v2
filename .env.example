# Environment Variables for CV Genius Project

# ========================================
# REQUIRED ENVIRONMENT VARIABLES
# ========================================

# Google Gemini AI Configuration (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Application Settings (REQUIRED)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=CV Genius

# ========================================
# DEVELOPMENT SETTINGS
# ========================================

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1
NODE_ENV=development

# ========================================
# OPTIONAL ENVIRONMENT VARIABLES
# ========================================

# Google AdSense Configuration (Optional - for monetization)
# Leave empty for development
NEXT_PUBLIC_ADSENSE_CLIENT=
NEXT_PUBLIC_ADSENSE_SIDEBAR_SLOT=
NEXT_PUBLIC_ADSENSE_INLINE_SLOT=
NEXT_PUBLIC_ADSENSE_FOOTER_SLOT=
NEXT_PUBLIC_ADSENSE_STICKY_SLOT=

# Analytics (Optional)
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# VERCEL_ANALYTICS_ID=your_vercel_analytics_id
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com

# Database Configuration (Optional - for future features)
# DATABASE_URL=your_database_url_here
# DATABASE_TOKEN=your_database_token_here

# ========================================
# PRODUCTION ONLY SETTINGS
# ========================================

# Authentication (Required for production admin panel)
# Generate a secure random string for production
NEXTAUTH_SECRET=your-secret-key-change-this-in-production
NEXTAUTH_URL=https://your-app.vercel.app

# Rate Limiting with Upstash Redis (Optional but recommended for production)
# UPSTASH_REDIS_REST_URL=your_upstash_redis_url
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Admin Panel Security Keys (Required for production admin access)
# Generate random hex values for security: https://passwordsgenerator.net/hex-generator/
# IMPORTANT: Change these values in production!
ADMIN_KEY_1=0x1A2B
ADMIN_KEY_2=0x3C4D
ADMIN_KEY_3=0x5E6F
ADMIN_KEY_4=0x7890

# JWT Configuration for Admin Panel (Required for production)
# IMPORTANT: Use a strong, unique secret in production (min 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-256-bits

# Admin Panel Credentials (Required for production)
# IMPORTANT: NEVER commit actual credentials to version control!
# Generate password hash: echo -n "your-secure-password" | openssl dgst -sha256
ADMIN_USERNAME=admin
# Generate with: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))" | base64
ADMIN_PWD_HASH_B64=your_base64_encoded_bcrypt_hash_here

# Admin Panel IP Whitelist (Required for production security)
# Comma-separated list of allowed IPs
ADMIN_IP_WHITELIST=127.0.0.1,::1,localhost

# Vercel API Configuration (Optional - for automatic deployments)
# Get from: https://vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_api_token_here
VERCEL_PROJECT_ID=your_vercel_project_id_here

# Password Encryption Key (Required for secure API communication)
# Generate: openssl rand -hex 32
PASSWORD_ENCRYPTION_KEY=generate_32_byte_hex_key_for_password_encryption

# ========================================
# EXAMPLE PRODUCTION CONFIGURATION
# ========================================
# Copy these settings for production deployment:
#
# NODE_ENV=production
# NEXT_PUBLIC_APP_URL=https://cvgenius.ie
# NEXT_PUBLIC_SITE_NAME=CV Genius Ireland
# NEXT_PUBLIC_GEMINI_API_KEY=your_actual_api_key
# NEXTAUTH_SECRET=generate-32-char-random-string
# JWT_SECRET=generate-another-32-char-random-string
# ADMIN_USERNAME=your_secure_admin_username
# ADMIN_PASSWORD_HASH=sha256_hash_of_your_secure_password
# ADMIN_KEY_1=0xA1B2C3D4
# ADMIN_KEY_2=0xE5F6G7H8
# ADMIN_KEY_3=0xI9J0K1L2
# ADMIN_KEY_4=0xM3N4O5P6

# ==============================================
# SECURITY CONFIGURATION
# ==============================================

# Security Audit Encryption (for audit logs)
AUDIT_ENCRYPTION_KEY=your-32-character-audit-encryption-key

# Disable IP whitelist for development (optional)
# DISABLE_IP_WHITELIST=true

# ==============================================
# ANALYTICS & MONITORING (OPTIONAL)
# ==============================================

# Google Analytics ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Vercel Analytics (automatically enabled on Vercel)
# NEXT_PUBLIC_VERCEL_ANALYTICS=true

# ==============================================
# IMPORTANT SECURITY NOTES:
# ==============================================
# 1. NEVER commit .env.local to git
# 2. Use strong, unique passwords and secrets
# 3. Rotate keys regularly in production
# 4. Keep admin access restricted to necessary IPs
# 5. Enable 2FA for admin accounts
# 6. Monitor audit logs regularly